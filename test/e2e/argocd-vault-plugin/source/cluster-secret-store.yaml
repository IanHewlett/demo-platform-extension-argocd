---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-admin
spec:
  provider:
    vault:
      server: <path:avp/data/test#sample>
      path: secret
      version: v2
      auth:
        kubernetes:
          mountPath: "ocal-minikube-us-east-0"
          role: "di-admin-kubernetes-role"
      namespace: admin
